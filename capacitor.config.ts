import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pokerogue.archipelago',
  appName: 'pokerogue archipelago',
  webDir: 'dist'
};

export default config;
